# ZTNA--platform-
A Zero Trust Network Access (ZTNA) platform is a security system that provides secure access to applications and network resources based on continuous verification of user identity and device security, rather than trusting users simply because they are inside the network.
